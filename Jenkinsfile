pipeline {
  agent any
  stages {
    stage('compile ') {
      steps {
        echo 'Compilation started..'
      }
    }
    stage('dosomething ') {
      steps {
        sh 'java -version '
      }
    }
  }
}